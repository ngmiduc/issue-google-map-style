import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'issue-google-maps-style',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
